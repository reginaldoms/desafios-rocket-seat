{% extends 'layout.njk' %}

{% block content %}
    <section>    
        <div class="cards" id="receitas">
            {% for item in items %}    
            <div class="card">
                <img src="{{item.foto}}" alt="Foto de Hamburguer">
                <h3>{{item.title}}</h3>
                <h5>{{item.author}}</h5>
            </div>                      
            {% endfor %} 
            {% for item in items %}
            <div class="card">
                <img src="{{item.foto}}" alt="Foto de Hamburguer">
                <h3>{{item.title}}</h3>
                <h5>{{item.author}}</h5>
            </div> 
            {% endfor %}                     
        </div>   
    </section>
    <div class="modal_overlay">
        <div class="modal">
            <a class="modal_close">
            <i>Fechar modal</i>
            </a>
            <div class="modal_content"></div>
        </div>
    </div>
    <script src='/scripts.js'></script>
{% endblock %}    