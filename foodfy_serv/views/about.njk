{% extends 'layout.njk' %}
{% block content%}
    <section>
        <div class="wrapper">
        <h1>As melhores receitas</h1>
        <p>Aprenda a construir os melhores pratos<br>com receitas criadas por profissionais<br>do mundo inteiro.</p>
        </div>

        <div class= "foto">
        <img src="./assets/chef.png" alt="Desenho do Chef">
        </div>

        <div class="title">
            <h2>Mais acessadas</h2>
        </div>    

        <div class="cards">
            {% for item in items %}
            <div class="card" id="{{item.id}}}">
                <img src="{{item.foto}}" alt="Foto de Hamburguer">
                <h3>{{item.title}}</h3>
                <h5>{{item.author}}</h5>
            </div> 
            {% endfor %}         
        </div>    
    </section>
  
{% endblock %}